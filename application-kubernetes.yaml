eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://eureka.default.svc.cluster.local:8760/eureka
  instance:
    hostname: ${HOSTNAME}.eureka
spring:    
  zipkin:
    base-url: http://zipkin-service-svc:80/
